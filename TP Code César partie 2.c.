// C++ code
//
int key;
int operation;
String message;

void setup()
{
   // ouverture de la liaison à une vitesse de 9600 bits par seconde.
    Serial.begin(9600);
  
  }  


String chiffrement(String message, int key) {
  String result = "" ;
  for (i = 0, i < message.length, i++ ) {
    char c = message[i];
    char chiffreChar = c + key;


}


String dechiffrement(String message, int key) {
  String result = "" ;
  for (i = 0, i < message.length, i++ )  {
    char c = message[i];
    char dechiffreChar = c - key;
      



}
void loop()
{
 
  digitalWrite(LED_BUILTIN, LOW);
  delay(1000); // Wait for 1000 millisecond(s)
} 
